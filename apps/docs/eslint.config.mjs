import { nextJsConfig } from "@workspace/eslint-config/next-js"

/** @type {import("eslint").Linter.Config[]} */
export default [
  ...nextJsConfig,
  // Generated by the fumadocs-mdx plugin (gitignored) — not ours to lint.
  { ignores: [".source/**"] },
]
