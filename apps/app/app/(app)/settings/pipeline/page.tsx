import { PacketPipelineSettings } from "@/components/settings/packet-pipeline-settings"

export const metadata = { title: "Settings · Packet pipeline" }

export default function SettingsPipelinePage() {
  return <PacketPipelineSettings />
}
